The app.py file is sitting outside the website folder.
It has all the imports from website folder.
In the terminal , just we need to run the command 'python app.py'
The command takes care of creation of database, no need to go to python shell and create the database.

By clicking on 127.0.0.1:5000 ( local host ), the app gets run. 

And then we explore the application .